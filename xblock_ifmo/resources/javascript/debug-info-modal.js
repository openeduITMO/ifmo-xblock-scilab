function DebugInfoModal(runtime, xblock, element, hooks, helpers) {

    var init = function ($, _)
    {
        console.log("DebugInfoModal initialization");
        var $modal = $(element);
        var id = $modal.data("id");


    };

    $(function(){
        init($, _);
    });

}